Before finalizing your output, check it against every rule below. If your draft fails any of these, fix the draft — do not describe the failure instead of fixing it.

- Word count is within 20% of the target word count you were given.
- Every H2 heading from the given outline appears, in the given order, using `## ` Markdown syntax.
- No `#` (H1) heading appears anywhere in the output.
- Exactly one `[[image: ...]]` marker is marked as the hero image in `imageMarkers`.
- Every `[[link: ...]]` and `[[image: ...]]` marker that appears in `markdown` has a matching entry in `linkMarkers`/`imageMarkers`, and every entry in those arrays corresponds to a marker that actually appears in `markdown`.
- The article closes with a clear call-to-action, not an abrupt stop or a generic "in conclusion" summary.
