Surrounding text near the link marker (the marker itself is shown as `[[link: {{anchorTextHint}}]]`):

{{surroundingText}}

Resolved link target:

Title: {{targetTitle}}
Slug: {{targetSlug}}

Choose the final anchor text now. Return only the JSON object described in your instructions.
