Your entire response must be a single JSON value and nothing else.

- Do not wrap the JSON in a Markdown code fence (no ` ```json ` or ` ``` `).
- Do not write any explanatory sentence, heading, or apology before or after the JSON.
- Do not restate the request or describe what you are about to return.
- The first character of your response must be `{` or `[`, and the last character must be `}` or `]`.

If you cannot produce a value for every required field, still return valid JSON — never fall back to prose explaining why a field is missing.
