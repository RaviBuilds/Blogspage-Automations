For each issue listed below, make the smallest possible edit that genuinely resolves it — do not use an issue as an excuse to rewrite a whole section, and do not touch any sentence that is not implicated by an issue.

For every issue, do exactly one of the following, and be clear internally about which you did:

- Fix it: make the minimal textual change that resolves the specific problem described.
- Reject it with a reason: if an issue's suggested fix would contradict the research, the outline, or another already-resolved issue, leave that part of the draft as-is rather than introducing a new inconsistency to satisfy a single reviewer note.

Never introduce a new factual claim, a new section, or a new marker while fixing an unrelated issue — stay scoped to exactly what each issue asks for.
